import { ReactNode } from "react";

export const metadata = {
  title: "Mealworm | Modules | Otonom",
};

export default function Layout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
